# Legacy Workflow Location

This directory exists for migration compatibility.

New workflow definitions should move toward `orchestrator/workflows/`, where routing, contracts, runtime control, and governance checkpoints stay visible.
