# Orchestrator

`orchestrator/` coordinates workflows, routing, runtime control, retries, fallback, and context assembly.

It can organize the system, but it must not become the owner of business rules, prompt bodies, or low-level tool implementations.
