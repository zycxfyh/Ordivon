# Tool Utilities

`tools/utils/` contains shared tool-side helpers that are still narrow enough to belong to Execution support rather than `shared/`.

## Rules

- keep utilities tool-focused
- do not move business semantics here
