# User Preferences Domain

`domains/userprefs/` is reserved for user operating preferences and defaults.

## Intended Use

- preference objects
- default behavior selection
- user-level configuration semantics

## Does Not Own

- secrets
- runtime infra config
- governance policy
