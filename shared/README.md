# Shared

`shared/` is intentionally narrow. Only place truly cross-layer primitives here: basic errors, config helpers, logging, time utilities, and generic value objects.

Business types do not belong in `shared/`.
