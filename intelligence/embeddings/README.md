# Intelligence Embeddings

`intelligence/embeddings/` is reserved for embedding-related runtime support.

## Intended Use

- vectorization helpers
- retrieval support embeddings
- future semantic indexing adapters
