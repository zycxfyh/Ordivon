# Intelligence

`intelligence/` owns model-facing capability: prompt templates, model routing, schemas, evaluators, embeddings, and reusable AI task definitions.

It does not own business state, API concerns, or governance enforcement.
