# Usage State

`state/usage/` owns operational usage snapshots.

This package is for:

- usage counters
- recent activity snapshots
- structured operational facts used for validation summaries

This package is not for:

- recommendation meaning
- review learning content
- governance policy decisions

Default rule:

If the object answers "what happened operationally and how much?", it likely belongs here.
