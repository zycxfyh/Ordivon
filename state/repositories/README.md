# State Repositories

`state/repositories/` is reserved for shared state-layer repository abstractions when they are not better placed in a specific domain.

## Rules

- prefer domain-specific repositories first
- only place cross-cutting state repository helpers here
