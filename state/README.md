# State

`state/` owns the system source of truth: schemas, repositories, migrations, snapshots, and state services.

If the question is "what is true right now?", the answer should resolve from here rather than from prompts, wiki notes, or cached agent memory.
