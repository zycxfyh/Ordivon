# State Seeds

`state/seeds/` is reserved for seed data used to initialize stateful environments.

## Rules

- seeds must remain obviously non-production where relevant
- seeds must not masquerade as canonical fact history
