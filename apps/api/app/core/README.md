# API Core

`apps/api/app/core/` holds API-app-local bootstrap and compatibility helpers.

## Intended Use

- app-local config bridges
- app-local DB helpers
- transitional compatibility code

## Boundary

Canonical shared config now lives outside this package.
