# API Schemas

`apps/api/app/schemas/` contains transport-facing request/response schemas.

## Owns

- FastAPI/Pydantic payload shapes
- transport validation
- HTTP contract vocabulary

## Does Not Own

- domain truth
- capability contracts
- ORM models
