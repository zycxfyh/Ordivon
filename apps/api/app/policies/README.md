# API Policies

`apps/api/app/policies/` is reserved for API-local policy glue only.

## Boundary

Canonical governance policy belongs in `governance/`.
Anything here should be transitional or transport-specific.
