# API Models

`apps/api/app/models/` is reserved for API-app-local model facades.

## Intended Use

- compatibility wrappers
- transport-adjacent modeling only

## Does Not Own

- domain truth models
- ORM ownership
