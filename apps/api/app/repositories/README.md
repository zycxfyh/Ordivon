# API Repositories

`apps/api/app/repositories/` is reserved for API-local compatibility repositories.

## Boundary

Canonical repositories should live in `domains/` or `state/`.
Anything here should be treated as migration-era glue.
