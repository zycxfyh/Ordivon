# App DB

`apps/db/` is reserved for app-facing database helpers that are specific to application bootstrapping or compatibility.

## Intended Use

- app-local DB glue
- transitional compatibility utilities

## Does Not Own

- canonical state schema
- domain repositories
- business persistence rules
